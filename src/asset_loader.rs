use crate::{model::Model, camera::ArcballCamera};

pub enum FileType {
    GLTF
}

pub struct AssetLoader;

impl AssetLoader {
    fn new() -> Self {
        Self {}
    }

    // fn load(&self, filename: String) -> (ArcballCamera, Model) {
        
    // }
}
